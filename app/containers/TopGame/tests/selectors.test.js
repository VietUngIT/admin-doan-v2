// import { fromJS } from 'immutable';
// import { makeSelectTopGameDomain } from '../selectors';

// const selector = makeSelectTopGameDomain();

describe('makeSelectTopGameDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
