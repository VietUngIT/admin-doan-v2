// import { fromJS } from 'immutable';
// import { makeSelectSearchGameDomain } from '../selectors';

// const selector = makeSelectSearchGameDomain();

describe('makeSelectSearchGameDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
