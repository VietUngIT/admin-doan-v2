// import { fromJS } from 'immutable';
// import { makeSelectRevenueDomain } from '../selectors';

// const selector = makeSelectRevenueDomain();

describe('makeSelectRevenueDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
