// import React from 'react';
// import { shallow } from 'enzyme';

// import { MessageTx } from '../index';

describe('<MessageTx />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
