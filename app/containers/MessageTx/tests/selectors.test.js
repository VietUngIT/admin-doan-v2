// import { fromJS } from 'immutable';
// import { makeSelectMessageTxDomain } from '../selectors';

// const selector = makeSelectMessageTxDomain();

describe('makeSelectMessageTxDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
