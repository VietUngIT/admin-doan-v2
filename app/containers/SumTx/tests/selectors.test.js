// import { fromJS } from 'immutable';
// import { makeSelectSumTxDomain } from '../selectors';

// const selector = makeSelectSumTxDomain();

describe('makeSelectSumTxDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
