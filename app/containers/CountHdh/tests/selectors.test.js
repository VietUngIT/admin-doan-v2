// import { fromJS } from 'immutable';
// import { makeSelectCountHdhDomain } from '../selectors';

// const selector = makeSelectCountHdhDomain();

describe('makeSelectCountHdhDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
