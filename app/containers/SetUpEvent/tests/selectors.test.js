// import { fromJS } from 'immutable';
// import { makeSelectSetUpEventDomain } from '../selectors';

// const selector = makeSelectSetUpEventDomain();

describe('makeSelectSetUpEventDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
