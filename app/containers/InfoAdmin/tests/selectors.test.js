// import { fromJS } from 'immutable';
// import { makeSelectInfoAdminDomain } from '../selectors';

// const selector = makeSelectInfoAdminDomain();

describe('makeSelectInfoAdminDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
