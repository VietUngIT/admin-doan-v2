// import { fromJS } from 'immutable';
// import { makeSelectSlotGameByNicknameDomain } from '../selectors';

// const selector = makeSelectSlotGameByNicknameDomain();

describe('makeSelectSlotGameByNicknameDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
