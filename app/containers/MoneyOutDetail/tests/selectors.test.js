// import { fromJS } from 'immutable';
// import { makeSelectMoneyOutDetailDomain } from '../selectors';

// const selector = makeSelectMoneyOutDetailDomain();

describe('makeSelectMoneyOutDetailDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
