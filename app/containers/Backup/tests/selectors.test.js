// import { fromJS } from 'immutable';
// import { makeSelectBackupDomain } from '../selectors';

// const selector = makeSelectBackupDomain();

describe('makeSelectBackupDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
