// import { fromJS } from 'immutable';
// import { makeSelectMarketInfoDetailDomain } from '../selectors';

// const selector = makeSelectMarketInfoDetailDomain();

describe('makeSelectMarketInfoDetailDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
