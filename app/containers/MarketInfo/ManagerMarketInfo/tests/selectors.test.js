// import { fromJS } from 'immutable';
// import { makeSelectManagerMarketInfoDomain } from '../selectors';

// const selector = makeSelectManagerMarketInfoDomain();

describe('makeSelectManagerMarketInfoDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
