// import { fromJS } from 'immutable';
// import { makeSelectListMarketInfoDomain } from '../selectors';

// const selector = makeSelectListMarketInfoDomain();

describe('makeSelectListMarketInfoDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
