// import { fromJS } from 'immutable';
// import { makeSelectTienHtDomain } from '../selectors';

// const selector = makeSelectTienHtDomain();

describe('makeSelectTienHtDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
