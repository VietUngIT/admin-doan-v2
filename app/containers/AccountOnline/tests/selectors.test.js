// import { fromJS } from 'immutable';
// import { makeSelectAccountOnlineDomain } from '../selectors';

// const selector = makeSelectAccountOnlineDomain();

describe('makeSelectAccountOnlineDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
