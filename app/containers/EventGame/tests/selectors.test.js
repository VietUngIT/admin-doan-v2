// import { fromJS } from 'immutable';
// import { makeSelectEventGameDomain } from '../selectors';

// const selector = makeSelectEventGameDomain();

describe('makeSelectEventGameDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
