// import { fromJS } from 'immutable';
// import { makeSelectMoneyTotalDomain } from '../selectors';

// const selector = makeSelectMoneyTotalDomain();

describe('makeSelectMoneyTotalDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
