// import { fromJS } from 'immutable';
// import { makeSelectMoneyHistoryDomain } from '../selectors';

// const selector = makeSelectMoneyHistoryDomain();

describe('makeSelectMoneyHistoryDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
