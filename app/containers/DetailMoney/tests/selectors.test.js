// import { fromJS } from 'immutable';
// import { makeSelectDetailMoneyDomain } from '../selectors';

// const selector = makeSelectDetailMoneyDomain();

describe('makeSelectDetailMoneyDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
