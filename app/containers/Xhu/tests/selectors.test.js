// import { fromJS } from 'immutable';
// import { makeSelectXhuDomain } from '../selectors';

// const selector = makeSelectXhuDomain();

describe('makeSelectXhuDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
