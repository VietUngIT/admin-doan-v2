// import { fromJS } from 'immutable';
// import { makeSelectPrizeGameDomain } from '../selectors';

// const selector = makeSelectPrizeGameDomain();

describe('makeSelectPrizeGameDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
