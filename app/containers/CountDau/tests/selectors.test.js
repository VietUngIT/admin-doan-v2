// import { fromJS } from 'immutable';
// import { makeSelectCountDauDomain } from '../selectors';

// const selector = makeSelectCountDauDomain();

describe('makeSelectCountDauDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
