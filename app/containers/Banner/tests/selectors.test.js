// import { fromJS } from 'immutable';
// import { makeSelectBannerDomain } from '../selectors';

// const selector = makeSelectBannerDomain();

describe('makeSelectBannerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
