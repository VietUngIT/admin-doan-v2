// import { fromJS } from 'immutable';
// import { makeSelectTopLooseDomain } from '../selectors';

// const selector = makeSelectTopLooseDomain();

describe('makeSelectTopLooseDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
