// import { fromJS } from 'immutable';
// import { makeSelectSetAdminDomain } from '../selectors';

// const selector = makeSelectSetAdminDomain();

describe('makeSelectSetAdminDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
