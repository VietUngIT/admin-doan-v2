// import { fromJS } from 'immutable';
// import { makeSelectEvenDomain } from '../selectors';

// const selector = makeSelectEvenDomain();

describe('makeSelectEvenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
