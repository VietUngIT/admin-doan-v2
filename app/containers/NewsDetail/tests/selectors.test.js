// import { fromJS } from 'immutable';
// import { makeSelectNewsDetailDomain } from '../selectors';

// const selector = makeSelectNewsDetailDomain();

describe('makeSelectNewsDetailDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
