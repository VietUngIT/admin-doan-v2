// import { fromJS } from 'immutable';
// import { makeSelectNotificationDomain } from '../selectors';

// const selector = makeSelectNotificationDomain();

describe('makeSelectNotificationDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
