// import { fromJS } from 'immutable';
// import { makeSelectMiniGameDomain } from '../selectors';

// const selector = makeSelectMiniGameDomain();

describe('makeSelectMiniGameDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
