// import { fromJS } from 'immutable';
// import { makeSelectCheckIpDomain } from '../selectors';

// const selector = makeSelectCheckIpDomain();

describe('makeSelectCheckIpDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
