// import { fromJS } from 'immutable';
// import { makeSelectCashOutDomain } from '../selectors';

// const selector = makeSelectCashOutDomain();

describe('makeSelectCashOutDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
