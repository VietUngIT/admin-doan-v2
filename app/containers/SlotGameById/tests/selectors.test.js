// import { fromJS } from 'immutable';
// import { makeSelectSlotGameByIdDomain } from '../selectors';

// const selector = makeSelectSlotGameByIdDomain();

describe('makeSelectSlotGameByIdDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
