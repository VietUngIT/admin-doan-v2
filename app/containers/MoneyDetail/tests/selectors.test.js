// import { fromJS } from 'immutable';
// import { makeSelectMoneyDetailDomain } from '../selectors';

// const selector = makeSelectMoneyDetailDomain();

describe('makeSelectMoneyDetailDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
