// import { fromJS } from 'immutable';
// import { makeSelectSmsactiveDomain } from '../selectors';

// const selector = makeSelectSmsactiveDomain();

describe('makeSelectSmsactiveDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
