// import { fromJS } from 'immutable';
// import { makeSelectMoneyContainerDomain } from '../selectors';

// const selector = makeSelectMoneyContainerDomain();

describe('makeSelectMoneyContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
