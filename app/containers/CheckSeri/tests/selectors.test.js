// import { fromJS } from 'immutable';
// import { makeSelectCheckSeriDomain } from '../selectors';

// const selector = makeSelectCheckSeriDomain();

describe('makeSelectCheckSeriDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
