// import { fromJS } from 'immutable';
// import { makeSelectListNewsDomain } from '../selectors';

// const selector = makeSelectListNewsDomain();

describe('makeSelectListNewsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
