// import { fromJS } from 'immutable';
// import { makeSelectBotDomain } from '../selectors';

// const selector = makeSelectBotDomain();

describe('makeSelectBotDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
