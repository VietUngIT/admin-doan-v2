// import { fromJS } from 'immutable';
// import { makeSelectMessegeDomain } from '../selectors';

// const selector = makeSelectMessegeDomain();

describe('makeSelectMessegeDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
