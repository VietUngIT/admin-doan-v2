// import { fromJS } from 'immutable';
// import { makeSelectDuyetCardDomain } from '../selectors';

// const selector = makeSelectDuyetCardDomain();

describe('makeSelectDuyetCardDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
