// import { fromJS } from 'immutable';
// import { makeSelectEventParentDomain } from '../selectors';

// const selector = makeSelectEventParentDomain();

describe('makeSelectEventParentDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
