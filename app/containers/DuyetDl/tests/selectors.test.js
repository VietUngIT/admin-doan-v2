// import { fromJS } from 'immutable';
// import { makeSelectDuyetDlDomain } from '../selectors';

// const selector = makeSelectDuyetDlDomain();

describe('makeSelectDuyetDlDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
