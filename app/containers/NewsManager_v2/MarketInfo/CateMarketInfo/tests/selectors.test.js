// import { fromJS } from 'immutable';
// import { makeSelectCateMarketInfoDomain } from '../selectors';

// const selector = makeSelectCateMarketInfoDomain();

describe('makeSelectCateMarketInfoDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
