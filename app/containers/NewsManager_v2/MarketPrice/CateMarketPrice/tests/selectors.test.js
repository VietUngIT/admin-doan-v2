// import { fromJS } from 'immutable';
// import { makeSelectCateMarketPriceDomain } from '../selectors';

// const selector = makeSelectCateMarketPriceDomain();

describe('makeSelectCateMarketPriceDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
