// import { fromJS } from 'immutable';
// import { makeSelectListMarketPriceDomain } from '../selectors';

// const selector = makeSelectListMarketPriceDomain();

describe('makeSelectListMarketPriceDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
