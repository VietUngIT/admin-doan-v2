// import { fromJS } from 'immutable';
// import { makeSelectListNewsEventDomain } from '../selectors';

// const selector = makeSelectListNewsEventDomain();

describe('makeSelectListNewsEventDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
