// import { fromJS } from 'immutable';
// import { makeSelectHelpPageDomain } from '../selectors';

// const selector = makeSelectHelpPageDomain();

describe('makeSelectHelpPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
