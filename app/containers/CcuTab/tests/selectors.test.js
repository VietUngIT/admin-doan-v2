// import { fromJS } from 'immutable';
// import { makeSelectCcuTabDomain } from '../selectors';

// const selector = makeSelectCcuTabDomain();

describe('makeSelectCcuTabDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
