import { createSelector } from 'reselect';

/**
 * Direct selector to the homePage1 state domain
 */
const selectHomePage1Domain = () => (state) => state.get('homePage1');

/**
 * Other specific selectors
 */


/**
 * Default selector used by HomePage1
 */



export {
  selectHomePage1Domain,
};
