// import { fromJS } from 'immutable';
// import { makeSelectManagerNewsDomain } from '../selectors';

// const selector = makeSelectManagerNewsDomain();

describe('makeSelectManagerNewsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
