// import { fromJS } from 'immutable';
// import { makeSelectTotalMoneyDomain } from '../selectors';

// const selector = makeSelectTotalMoneyDomain();

describe('makeSelectTotalMoneyDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
