// import { fromJS } from 'immutable';
// import { makeSelectGittCodeDomain } from '../selectors';

// const selector = makeSelectGittCodeDomain();

describe('makeSelectGittCodeDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
